<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>
<?import com.disaster.alert.util.ParticleNetworkCanvas?>

<StackPane xmlns="http://javafx.com/javafx/17"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="com.disaster.alert.controller.LoginController"
           styleClass="login-root"
           stylesheets="@/css/login.css"
           maxWidth="Infinity"
           maxHeight="Infinity">

    <!-- Particle network animation layer -->
    <ParticleNetworkCanvas/>

    <!-- Centered content -->
    <VBox alignment="CENTER" spacing="0">

        <!-- Glass login card -->
        <VBox styleClass="glass-card"
              alignment="CENTER"
              spacing="20">
            <padding>
                <Insets top="40" right="40" bottom="40" left="40"/>
            </padding>

            <!-- Logo badge -->
            <VBox styleClass="login-logo-box" alignment="CENTER" spacing="4">
                <Label text="&#127757;" style="-fx-font-size: 32px;"/>
                <Label text="NADAS" styleClass="login-brand"/>
            </VBox>

            <!-- Title -->
            <VBox spacing="6" alignment="CENTER">
                <Label text="Disaster Alert System" styleClass="login-title">
                    <font><Font name="System Bold" size="26"/></font>
                </Label>
                <Label text="Sign in to your account" styleClass="login-subtitle"/>
            </VBox>

            <Separator styleClass="login-divider" maxWidth="Infinity"/>

            <!-- Email field -->
            <VBox spacing="7" maxWidth="Infinity">
                <Label text="Email Address" styleClass="login-field-label">
                    <font><Font name="System Bold" size="12"/></font>
                </Label>
                <TextField fx:id="emailField"
                           promptText="Enter your email"
                           styleClass="login-field"
                           maxWidth="Infinity">
                    <font><Font size="13"/></font>
                </TextField>
            </VBox>

            <!-- Password field -->
            <VBox spacing="7" maxWidth="Infinity">
                <Label text="Password" styleClass="login-field-label">
                    <font><Font name="System Bold" size="12"/></font>
                </Label>
                <PasswordField fx:id="passwordField"
                               promptText="Enter your password"
                               styleClass="login-field"
                               maxWidth="Infinity">
                    <font><Font size="13"/></font>
                </PasswordField>
            </VBox>

            <!-- Error label -->
            <Label fx:id="errorLabel"
                   text=""
                   styleClass="login-error"
                   wrapText="true"
                   visible="false"
                   maxWidth="Infinity">
                <font><Font size="12"/></font>
            </Label>

            <!-- Login button -->
            <Button fx:id="loginButton"
                    text="Sign In"
                    onAction="#handleLogin"
                    styleClass="login-btn"
                    maxWidth="Infinity">
                <font><Font name="System Bold" size="14"/></font>
            </Button>

            <!-- Register link -->
            <HBox alignment="CENTER" spacing="5">
                <Label text="Don't have an account?" styleClass="login-small-text"/>
                <Hyperlink fx:id="registerLink"
                           text="Register Now"
                           onAction="#handleRegisterLink"
                           styleClass="login-register-link">
                    <font><Font name="System Bold" size="12"/></font>
                </Hyperlink>
            </HBox>

        </VBox>

        <!-- Bottom brand tag -->
        <Label text="Natural &amp; Artificial Disaster Alert System  •  v1.0"
               style="-fx-text-fill: rgba(100,180,255,0.3); -fx-font-size: 10px;"
               alignment="CENTER">
            <VBox.margin><Insets top="16"/></VBox.margin>
        </Label>

    </VBox>
</StackPane>